@model Notes.Controllers.ApplicationUserEditForm

@{
    ViewBag.Title = "Configure user as a manager";
}

<h2>Configure user as a Manager</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Decide, then click the Yes or No button</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.FullName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.FullName, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
                @Html.ValidationMessageFor(model => model.FullName, "", new { @class = "text-danger" })
            </div>
        </div>

        @*<div class="form-group">
            <label class="control-label col-md-2" for="Manager">Is a Manager?</label>
            <div class="col-md-10">
                @{
    foreach (var item in Model.IsManager)
    {
        <div class="checkbox">
            <label>
                <input type="checkbox" name="Manager" value="@item.Text"
                       checked="@(item.Selected)" />@item.Text
                </label>
            </div>
    }
                }
            </div>
        </div>*@


        <div class="form-group">
            <label class="control-label col-md-2" for="Manager">Configure as Manager?</label>
            <div class="col-md-10">
                <input type="submit" value="Yes" class="btn btn-success" />
                @Html.ActionLink("No", "NonManagers", null, new { @class = "btn btn-danger" })
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "NonManagers")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
